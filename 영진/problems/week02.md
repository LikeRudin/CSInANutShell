sky
```
1. 캐시의 공간 지역성과 시간 지역성에 대해 설명하시오

2. 캐시의 3가지 매핑 방법과 장단점을 서술하시오

👀
3. 아래 문제에서 대략 예상되는 정답과 그것이 발생하는 이유를 설명하시오

let floatResult = 0.0;
for(let i=0;i<100000;i++) {
    floatResult += 0.00001;
}

console.log(floatResult);
```

bee

```
1. Set-associative mapping이 Direct mapping 보다 충돌을 덜 일으키면서도 캐시 크기와 성능을 유지할 수 있나요?

2. RAID 5가 RAID 1에 비해 안정성을 높이면서도 용량을 효율적으로 사용할 수 있는 까닭은 무엇인가요?

👀
3. 짝수 패리티의 해밍 코드가 0011011일때 오류가 수정된 코드는 무엇인가요?
```

choi

```
1. 캐시메모리는 왜 사용되나요?

👀
2. 메인메모리에서 캐시에 저장될 때의 구조는?

3. 해밍코드는 어떤 방식으로 오류를 수정하나요?
```



---

Answer

sky
```
3. 아래 문제에서 대략 예상되는 정답과 그것이 발생하는 이유를 설명하시오

let floatResult = 0.0;
for(let i=0;i<100000;i++) {
    floatResult += 0.00001;
}

console.log(floatResult);
```
```
자바스크립트는 부동 소수점방식을 이용하여 수를표현하기때문에
0.00001의 근사값을 반복해서 더합니다.

따라서 오차가 발생해서 1보다 크거나 작은값이 나올것입니다.

const floatTest = () => {
    let floatResult = 0.0;
    for(let i=0;i<100000;i++) {
    floatResult += 0.00001;}
    return floatResult}

그런데 위함수의 실행결과는 항상 이값으로 나오네요
조사가 필요합니다.
0.9999999999980838
```

bee
```
짝수 패리티의 해밍 코드가 0011011일때 오류가 수정된 코드는 무엇인가요?

```

choi
```
메인메모리에서 캐시에 저장될 때의 구조는?

하나의 메모리에대해 하나의 라인이라는 블록에 저장

라인마다 고유의 태그값에 따라 메인메모리의 데이터를 저장
```